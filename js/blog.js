 document.onreadystatechange = function () {
    if (document.readyState == "complete") {
        // load data
        console.log(CONTENT);
   }
 };

 function generatePost(post) {
    // make a DOM object
    // inject the content into it
    // inject that into the DOM
 }